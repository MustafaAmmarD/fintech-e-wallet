/**
 * Identity module — User registration, authentication, and profile management.
 * <p>
 * Domain layer: pure Java entities and repository ports (no framework
 * annotations).
 */
package com.fintech.ewallet.identity.domain;
